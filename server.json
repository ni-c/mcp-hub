{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.ni-c/mcp-hub",
  "title": "mcp-hub",
  "description": "Serve many stdio MCP servers from one container, for ChatGPT, Claude and other MCP clients",
  "websiteUrl": "https://mcp-hub.ni-c.de",
  "repository": {
    "url": "https://github.com/ni-c/mcp-hub",
    "source": "github"
  },
  "version": "0.5.1",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@ni-c/mcp-hub",
      "version": "0.5.1",
      "transport": {
        "type": "streamable-http",
        "url": "http://localhost:{PORT}/hub"
      },
      "environmentVariables": [
        {
          "name": "EXTERNAL_URL",
          "description": "Public base URL exactly as clients see it, e.g. https://mcp.example.net (no path)",
          "isRequired": true,
          "format": "string"
        },
        {
          "name": "PASSWORD_HASH",
          "description": "bcrypt hash of the OAuth login password (htpasswd -bnBC 10 \"\" 'pw' | tr -d ':\\n')",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "CONFIG_PATH",
          "description": "Path to the mcp.json listing the servers to expose, in Claude Code's mcpServers format (default /config/mcp.json)",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "DATA_PATH",
          "description": "Directory for the JWT key and OAuth client state; must persist across restarts (default /data)",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "TRUSTED_PROXIES",
          "description": "Comma-separated IPs/CIDRs of the reverse proxy allowed to set X-Forwarded-* headers",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "PORT",
          "description": "Listen port",
          "isRequired": false,
          "format": "number",
          "default": "3000"
        }
      ]
    },
    {
      "registryType": "oci",
      "identifier": "ghcr.io/ni-c/mcp-hub:0.5.1",
      "transport": {
        "type": "streamable-http",
        "url": "http://localhost:{PORT}/hub"
      },
      "environmentVariables": [
        {
          "name": "EXTERNAL_URL",
          "description": "Public base URL exactly as clients see it, e.g. https://mcp.example.net (no path)",
          "isRequired": true,
          "format": "string"
        },
        {
          "name": "PASSWORD_HASH",
          "description": "bcrypt hash of the OAuth login password (htpasswd -bnBC 10 \"\" 'pw' | tr -d ':\\n')",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "TRUSTED_PROXIES",
          "description": "Comma-separated IPs/CIDRs of the reverse proxy allowed to set X-Forwarded-* headers",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "PORT",
          "description": "Listen port inside the container",
          "isRequired": false,
          "format": "number",
          "default": "80"
        }
      ]
    }
  ]
}
