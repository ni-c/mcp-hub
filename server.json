{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.ni-c/mcp-hub",
  "title": "mcp-hub",
  "description": "Serve many stdio MCP servers from one container with CIMD-first OAuth 2.1, for ChatGPT, Claude and other MCP clients",
  "websiteUrl": "https://mcp-hub.ni-c.de",
  "repository": {
    "url": "https://github.com/ni-c/mcp-hub",
    "source": "github"
  },
  "version": "0.5.1",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@ni-c/mcp-hub",
      "version": "0.5.1",
      "runtimeHint": "npx",
      "transport": {
        "type": "stdio"
      },
      "packageArguments": [
        {
          "type": "positional",
          "value": "--stdio"
        }
      ],
      "environmentVariables": [
        {
          "name": "CONFIG_PATH",
          "description": "Path to the mcp.json listing the servers to expose, in Claude Code's mcpServers format (default mcp.json in the working directory)",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "IDLE_TIMEOUT_MINUTES",
          "description": "Minutes of inactivity before a server is put to sleep; 0 starts every server at launch and keeps it running (default 60)",
          "isRequired": false,
          "format": "number",
          "default": "60"
        },
        {
          "name": "TOOL_CACHE_PATH",
          "description": "Tool snapshots of sleeping servers (default .mcp-hub/tool-cache.json beside the config)",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "LOG_FILE",
          "description": "Mirror the hub's log lines into this file; stdout carries the protocol, so logging goes to stderr otherwise",
          "isRequired": false,
          "format": "string"
        }
      ]
    },
    {
      "registryType": "oci",
      "identifier": "ghcr.io/ni-c/mcp-hub:0.5.1",
      "transport": {
        "type": "streamable-http",
        "url": "http://localhost:{PORT}/hub"
      },
      "environmentVariables": [
        {
          "name": "EXTERNAL_URL",
          "description": "Public base URL exactly as clients see it, e.g. https://mcp.example.net (no path)",
          "isRequired": true,
          "format": "string"
        },
        {
          "name": "PASSWORD_HASH",
          "description": "bcrypt hash of the OAuth login password (htpasswd -bnBC 10 \"\" 'pw' | tr -d ':\\n')",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "TRUSTED_PROXIES",
          "description": "Comma-separated IPs/CIDRs of the reverse proxy allowed to set X-Forwarded-* headers",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "CLIENT_REGISTRATION",
          "description": "Which mechanisms a client may use to obtain a client_id: cimd (Client ID Metadata Documents), dcr (RFC 7591 dynamic registration), or both",
          "isRequired": false,
          "format": "string",
          "default": "cimd,dcr"
        },
        {
          "name": "CIMD_ALLOWED_ORIGINS",
          "description": "Comma-separated https origins whose client ID metadata documents are accepted, e.g. https://chatgpt.com; unset admits every https origin",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "PORT",
          "description": "Listen port inside the container",
          "isRequired": false,
          "format": "number",
          "default": "80"
        }
      ]
    }
  ]
}
