import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.qcK96eCy.js";var i=JSON.parse(`{"title":"Elicitation","description":"","frontmatter":{},"headers":[],"relativePath":"guide/elicitation.md","filePath":"guide/elicitation.md","lastUpdated":1788439235000}`),a={name:`guide/elicitation.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="elicitation" tabindex="-1">Elicitation <a class="header-anchor" href="#elicitation" aria-label="Permalink to “Elicitation”">​</a></h1><p>Some servers need a person, not a model. <code>smtp-mcp</code> asks before it sends a message; <code>imap-mcp</code> asks before it expunges a mailbox. MCP calls that <strong>elicitation</strong>: the server puts a question in front of whoever is sitting at the client, and the model cannot answer on their behalf.</p><p>Behind a gateway that question used to have nowhere to go, and a server that cannot ask does not stop working — it falls back to something weaker. Both of those two fall back to a confirmation token returned inside a tool result, which the model reads and can satisfy by calling twice in the same turn. That still catches a mistake. It does not catch a model that has been talked into something.</p><p>On the <code>2026-07-28</code> revision the hub carries the question through, in both directions.</p><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to “How it works”">​</a></h2><p>The reason this fits a stateless hub is that the question is not a push. On <code>2026-07-28</code> there is no server→client request channel at all: a server answers <code>input_required</code> instead of a result, and the call <strong>ends</strong>.</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>client ──tools/call──► hub ──────► child</span></span>
<span class="line"><span>                                     │ &quot;may I really delete these?&quot;</span></span>
<span class="line"><span>client ◄──input_required── hub ◄─────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                        ( the person reads it and decides )</span></span>
<span class="line"><span></span></span>
<span class="line"><span>client ──tools/call + the answer──► hub ──────► child</span></span>
<span class="line"><span>client ◄──────── the result ─────── hub ◄───────┘</span></span></code></pre></div><p>Nothing is held open while somebody thinks. No request occupies one of the hub&#39;s <a href="/guide/security">concurrency slots</a>, no timer runs, and the five-minute call deadline is never anywhere near being reached — the hub→child leg is two short calls, not one long one. Statelessness is the reason this works rather than an obstacle to it.</p><h2 id="what-the-hub-adds" tabindex="-1">What the hub adds <a class="header-anchor" href="#what-the-hub-adds" aria-label="Permalink to “What the hub adds”">​</a></h2><p>The question was written by a child server and is shown to a human as though the hub were asking. That crosses a trust boundary, so four things happen to it on the way through, and each is a refusal rather than a repair.</p><p><strong>It is attributed.</strong> The message is prefixed with <code>Server &quot;&lt;name&gt;&quot; asks:</code>, and the text is stripped of bidirectional overrides, zero-width and control characters first — otherwise a child could visually reverse the very line that names it.</p><p><strong>Only questions travel.</strong> A child may embed other requests in an <code>input_required</code>; the hub carries <code>elicitation/create</code> and drops the rest. An embedded <code>sampling/createMessage</code> would spend the caller&#39;s model budget on a child&#39;s prompt, and <code>roots/list</code> would hand a child the client&#39;s workspace layout. Neither is something to relay on a child&#39;s say-so. What was dropped is named in the hub log.</p><p><strong>The child&#39;s <code>_meta</code> is removed.</strong> A progress token or a related-task id belongs to the child&#39;s own id space; forwarded downstream it would collide with the client&#39;s.</p><p><strong>The state is sealed.</strong> <code>requestState</code> goes out to the client and comes back, so the hub treats it as attacker-controlled: it is signed with the same secret that signs the login cookie and bound to the server, the tool, the OAuth client and the endpoint it was minted at. A state that does not open — expired, out of rounds, forged, or minted for a different call — is refused as a whole, without saying which part was wrong.</p><h2 id="when-the-hub-asks-nothing" tabindex="-1">When the hub asks nothing <a class="header-anchor" href="#when-the-hub-asks-nothing" aria-label="Permalink to “When the hub asks nothing”">​</a></h2><p>The hub only announces to a child that a question is answerable when it actually is. All four have to hold at once:</p><ol><li>the operator has not switched pass-through off, globally or for this server;</li><li>the client declared <code>elicitation</code> <strong>in this request</strong> — which is a <code>2026-07-28</code> thing, so a 2025 client is ruled out here;</li><li>the child negotiated <code>2026-07-28</code>, so its answer can be a result;</li><li>the child actually asked something.</li></ol><p>The capability is mirrored from what the client declared for that one call and never widened. That is what keeps the announcement honest: it says only &quot;the caller of this one call can answer you&quot;.</p><p>A 2025 client over HTTP therefore never sees an elicitation, and the child is told so rather than left to discover it — it takes its own fallback, which is the correct behaviour for a client that genuinely cannot be asked. Over stdio there is no such limit: <code>mcp-hub-stdio</code> is spawned per client session, so both eras reach a person.</p><h3 id="finding-out-why-nothing-was-asked" tabindex="-1">Finding out why nothing was asked <a class="header-anchor" href="#finding-out-why-nothing-was-asked" aria-label="Permalink to “Finding out why nothing was asked”">​</a></h3><p>A child&#39;s fallback is deliberately quiet, and that is the problem: a server that took its two-call token instead of asking looks exactly like a server that had nothing to ask. Two ways to tell them apart.</p><p>From the operator&#39;s side, the hub says it once per client, server and reason:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>mcp-hub: [freshrss] elicitation not forwarded for client &quot;…&quot; — the caller</span></span>
<span class="line"><span>declared no elicitation capability for this request, so a question would have</span></span>
<span class="line"><span>nowhere to go</span></span></code></pre></div><p>Once, not per call: a connector that cannot be asked calls all day and the answer never changes.</p><p>From inside the client, switch <a href="/reference/environment#diagnostics"><code>MCP_DIAGNOSTICS</code></a> on and ask <a href="/reference/hub-tools#describe-connection"><code>describe_connection</code></a> — it reports the era, the capability this request carried, and for a named server whether a question would reach you.</p><h2 id="switching-it-off" tabindex="-1">Switching it off <a class="header-anchor" href="#switching-it-off" aria-label="Permalink to “Switching it off”">​</a></h2><p>Per server, in <code>mcp.json</code>:</p><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">{</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">  &quot;mcpServers&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: {</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">    &quot;imap&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: { </span><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">&quot;command&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;npx&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">, </span><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">&quot;args&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: [</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;-y&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">, </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;@ni-c/imap-mcp&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">], </span><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">&quot;passthrough&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;off&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;"> }</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">  }</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">}</span></span></code></pre></div><p><code>&quot;off&quot;</code> withdraws that upstream&#39;s right to put words in front of the user. It is a phishing judgement, not an availability one: the server keeps working and falls back on its own. <code>&quot;auto&quot;</code> is the default.</p><p>Globally, <code>MCP_ELICITATION=false</code> is the emergency brake. The rest of the limits exist so that one child cannot turn a prompt into a denial of service:</p><table tabindex="0"><thead><tr><th>Variable</th><th>Default</th><th>What it bounds</th></tr></thead><tbody><tr><td><code>MCP_ELICITATION</code></td><td><code>true</code></td><td>the whole feature</td></tr><tr><td><code>MCP_ELICITATION_MAX_ROUNDS</code></td><td><code>8</code></td><td>how often one call may come back for more input</td></tr><tr><td><code>MCP_ELICITATION_STATE_TTL_MS</code></td><td><code>900000</code></td><td>how long a half-finished call stays resumable</td></tr><tr><td><code>MCP_ELICITATION_MAX_MESSAGE_BYTES</code></td><td><code>4096</code></td><td>one prompt — anything longer is not a prompt</td></tr><tr><td><code>MCP_ELICITATION_MAX_PAYLOAD_BYTES</code></td><td><code>131072</code></td><td>the whole question, schemas included</td></tr></tbody></table><p>The round cap is enforced through the sealed state rather than a counter, because the hub keeps nothing between requests.</p><h2 id="what-is-not-logged" tabindex="-1">What is not logged <a class="header-anchor" href="#what-is-not-logged" aria-label="Permalink to “What is not logged”">​</a></h2><p>Never the question and never the answer. Both are text a person read or typed; what the log gets is the server name, the tool and the fact that something was dropped.</p><h2 id="what-this-is-worth-next-to-an-annotation" tabindex="-1">What this is worth, next to an annotation <a class="header-anchor" href="#what-this-is-worth-next-to-an-annotation" aria-label="Permalink to “What this is worth, next to an annotation”">​</a></h2><p>A child&#39;s <a href="/reference/hub-tools#list-tools">tool annotations</a> travel through the hub unchanged, and they are a <strong>hint a client may ignore</strong> — the specification says so in as many words. Elicitation is not: the child raises the question, and until an answer comes back it does nothing.</p><p>That is why passing it through matters more than it looks. A server whose <code>delete_*</code> tool asks a person keeps asking behind the hub; one that is only annotated <code>destructiveHint: true</code> is relying on a client to notice. The servers in <a href="https://github.com/ni-c" target="_blank" rel="noreferrer">this family</a> do the former — see, for instance, <a href="https://imap-mcp.ni-c.de/guide/approval" target="_blank" rel="noreferrer">imap-mcp&#39;s approval guide</a> — and each of them can be switched onto its two-call token fallback with <code>ELICITATION=false</code> if a dialog is the wrong shape for a deployment.</p><p>Note the two names are deliberately different. <code>MCP_ELICITATION</code> here switches off the hub&#39;s <strong>carrying</strong> of the question; <code>ELICITATION</code> in a child switches off that child&#39;s <strong>asking</strong> of it. Two layers, two switches — and neither of them turns a guarded call into an unguarded one.</p><h2 id="next" tabindex="-1">Next <a class="header-anchor" href="#next" aria-label="Permalink to “Next”">​</a></h2><ul><li><a href="/reference/hub-tools#list-tools">Hub meta-tools</a> — what a child&#39;s annotations look like coming out of <code>list_tools</code></li><li><a href="/reference/standards#transport-and-protocol">Standards</a> — the full capability matrix, per revision</li><li><a href="/guide/architecture#stateless-transport">Architecture</a> — why the stateless transport helps here and hurts elsewhere</li><li><a href="/guide/configuration">Configuration</a> — where <code>passthrough</code> goes</li></ul>`,40)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};