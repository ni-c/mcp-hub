import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.qcK96eCy.js";var i=JSON.parse(`{"title":"Client compatibility","description":"","frontmatter":{},"headers":[],"relativePath":"guide/client-compatibility.md","filePath":"guide/client-compatibility.md","lastUpdated":1788301578000}`),a={name:`guide/client-compatibility.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="client-compatibility" tabindex="-1">Client compatibility <a class="header-anchor" href="#client-compatibility" aria-label="Permalink to “Client compatibility”">​</a></h1><p>mcp-hub speaks Streamable HTTP behind OAuth 2.1 — and, since 0.6.0, behind admin-minted API tokens for clients that cannot do OAuth. This page says, per client, which path to use and which quirks to expect.</p><p>The ecosystem falls into three camps.</p><h2 id="camp-1-—-oauth-clients-use-the-normal-flow" tabindex="-1">Camp 1 — OAuth clients (use the normal flow) <a class="header-anchor" href="#camp-1-—-oauth-clients-use-the-normal-flow" aria-label="Permalink to “Camp 1 — OAuth clients (use the normal flow)”">​</a></h2><p>These implement the MCP authorization spec: they discover the hub through the <code>.well-known</code> documents, obtain a <code>client_id</code>, open a browser for the password login, and refresh silently. <a href="/guide/clients">Connecting clients</a> covers the general flow; below are the per-client notes.</p><p>Since 0.10.0 the hub accepts both registration mechanisms — <a href="/guide/client-registration#client-id-metadata-documents">Client ID Metadata Documents</a> and <a href="/guide/client-registration#dynamic-client-registration">dynamic registration</a> — and advertises both, so each client takes whichever path it prefers. The notes below say which one that is where it matters.</p><table tabindex="0"><thead><tr><th>Client</th><th>Notes</th></tr></thead><tbody><tr><td><strong>Claude Web / Claude Code</strong></td><td>Reference behaviour; sends the <code>resource</code> parameter, loopback redirects match any port.</td></tr><tr><td><strong>Cursor</strong></td><td>Sends <code>resource</code>; uses fixed redirect URIs (desktop <code>http://localhost:8787/callback</code>, web <code>https://www.cursor.com/agents/mcp/oauth/callback</code>) — registered automatically via DCR.</td></tr><tr><td><strong>Mistral Le Chat</strong></td><td>Auto-detects OAuth from the 401 <code>WWW-Authenticate</code> challenge; add the connector URL, done.</td></tr><tr><td><strong>LibreChat</strong></td><td>Works via DCR. <strong>If you use a static header instead, set <code>requiresOAuth: false</code></strong> — its auto-detection probes without your headers, sees the 401 and misclassifies the server as OAuth-only.</td></tr><tr><td><strong>Open WebUI</strong> ≥ 0.6.31</td><td>Choose &quot;OAuth 2.1 (DCR)&quot;. Set <code>WEBUI_SECRET_KEY</code>, or tokens break on every container restart. If OAuth fails with a resource error, set the <strong>OAuth Resource Parameter</strong> option to <em>Include</em>.</td></tr><tr><td><strong>Gemini CLI</strong></td><td>Use <code>httpUrl</code> (not <code>url</code>) for Streamable HTTP. Its resource check compares strictly — connect to the exact URL the path-scoped metadata names, e.g. <code>https://…/paperless/mcp</code>, not a shortened form. <a href="/guide/clients#gemini-cli">Step by step</a>.</td></tr><tr><td><strong>qwen-code</strong></td><td>Also <code>httpUrl</code>. OAuth callback is fixed to port 7777 (<code>--oauth-redirect-uri</code> overrides).</td></tr><tr><td><strong>Kimi Code CLI</strong></td><td><code>kimi mcp add --transport http --auth oauth &lt;name&gt; &lt;url&gt;</code>; re-registers with a fresh random loopback port per flow, which the hub accepts.</td></tr><tr><td><strong>VS Code (GitHub Copilot)</strong></td><td><code>type: &quot;http&quot;</code> in <code>.vscode/mcp.json</code>; OAuth automatic. Recent builds identify themselves with the metadata document at <code>https://vscode.dev/oauth/client-metadata.json</code>; older ones register dynamically. Either way the redirects are <code>http://127.0.0.1:33418</code> and <code>https://vscode.dev/redirect</code>.</td></tr><tr><td><strong>Codex CLI</strong></td><td>Pass the resource when adding the server: <code>codex mcp add &lt;name&gt; --url &lt;url&gt; --oauth-resource &lt;url&gt;</code>; builds without that flag need <code>oauth_resource</code> written into <code>config.toml</code> by hand. Older Codex builds omit the resource on refresh — the hub tolerates that and keeps the binding from the original grant. <a href="/guide/clients#codex-cli">Step by step</a>.</td></tr><tr><td><strong>ChatGPT connectors</strong></td><td>Both paths work (developer mode → custom connector), but the OAuth endpoints have to be filled in by hand. CIMD with <code>private_key_jwt</code> is its preferred path and is supported since 0.10.0 — the per-connector document URL is random, so allowlist the origin <code>https://chatgpt.com</code>, never an exact URL. On the DCR path the hub plays along with its quirks: public clients get a non-expiring <code>client_secret</code> in the response. An approved registration is kept as long as it is used; see the <a href="/guide/client-registration#registrations-do-not-accumulate">lifecycle rules</a>. <a href="/guide/clients#chatgpt">Step by step</a>.</td></tr><tr><td><strong>Copilot Studio / M365 Copilot</strong></td><td>&quot;Dynamic discovery&quot; mode should work since the hub now issues client secrets on registration. Untested — reports welcome.</td></tr></tbody></table><h3 id="which-mcp-revision-a-client-speaks" tabindex="-1">Which MCP revision a client speaks <a class="header-anchor" href="#which-mcp-revision-a-client-speaks" aria-label="Permalink to “Which MCP revision a client speaks”">​</a></h3><p>Nothing on this page depends on it. The hub answers <code>2026-07-28</code> and <code>2025-11-25</code> on every endpoint, the client picks during its opening exchange, and it cannot tell from the answers which one it got — that is the whole point of the <a href="/reference/standards#what-is-carried-per-revision">capability matrix</a>.</p><p>One feature does depend on it: <a href="/guide/elicitation">elicitation</a>, where a server asks the person at the far end a question. Over HTTP that needs a client on <code>2026-07-28</code>.</p><table tabindex="0"><thead><tr><th>Client</th><th>Revision</th><th>How we know</th></tr></thead><tbody><tr><td><strong>Claude Code</strong></td><td><code>2026-07-28</code>, fully</td><td>Measured in the shipped binary: the multi-round-trip driver, <code>server/discover</code>, the per-request capability envelope and both typed error codes are all present.</td></tr><tr><td>everything else here</td><td>unmeasured</td><td>Assume <code>2025-11-25</code> until it is checked. Nothing breaks either way; a question from a server simply does not reach you, and the server falls back on its own.</td></tr></tbody></table><p>This table stays honest by being short. A client is listed as speaking the 2026 revision when somebody has watched it do so, not when its release notes say it should.</p><p>Clients that omit the RFC 8707 <code>resource</code> parameter entirely are refused by default (<code>invalid_target</code>). If you need to serve such a client, set <a href="/reference/environment"><code>DEFAULT_RESOURCE</code></a> — tokens are then bound to that one resource instead of rejected. They are never global.</p><h2 id="camp-2-—-api-clients-use-an-api-token" tabindex="-1">Camp 2 — API clients (use an API token) <a class="header-anchor" href="#camp-2-—-api-clients-use-an-api-token" aria-label="Permalink to “Camp 2 — API clients (use an API token)”">​</a></h2><p>These products connect <strong>server-side</strong> and can only pass static headers — no browser, no OAuth. Mint them a long-lived token bound to one resource:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#953800;--shiki-dark:#FFA657;">docker</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> compose</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> stop</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> mcp-hub</span></span>
<span class="line"><span style="--shiki-light:#953800;--shiki-dark:#FFA657;">docker</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> compose</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> run</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF;"> --rm</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF;"> --no-deps</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> mcp-hub</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;"> \\</span></span>
<span class="line"><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">  node</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> /app/dist/admin.js</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> tokens</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> create</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF;"> --resource</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> hub</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF;"> --days</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF;"> 90</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF;"> --label</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> &quot;openai&quot;</span></span>
<span class="line"><span style="--shiki-light:#953800;--shiki-dark:#FFA657;">docker</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> compose</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> up</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF;"> -d</span></span></code></pre></div><p>The token is printed exactly once. <code>tokens list</code> shows the records, <code>tokens revoke &lt;id&gt;</code> kills one immediately.</p><div class="warning custom-block"><p class="custom-block-title">API tokens trade rotation for compatibility</p><p>No refresh, no rotation — anyone holding the token has full access to its resource until expiry or revocation. Keep lifetimes short (90 days is a sensible default), one token per integration so revocation is surgical, and treat the value like a password.</p></div><h3 id="openai-responses-api" tabindex="-1">OpenAI Responses API <a class="header-anchor" href="#openai-responses-api" aria-label="Permalink to “OpenAI Responses API”">​</a></h3><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">{</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">  &quot;model&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;gpt-5.2&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">,</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">  &quot;tools&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: [{</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">    &quot;type&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;mcp&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">,</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">    &quot;server_label&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;mcp-hub&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">,</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">    &quot;server_url&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;https://mcp.example.net/hub&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">,</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">    &quot;authorization&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;&lt;token&gt;&quot;</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">  }],</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">  &quot;input&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;…&quot;</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">}</span></span></code></pre></div><p>The <code>authorization</code> value is not stored by OpenAI and must be sent on every request.</p><h3 id="xai-grok-api" tabindex="-1">xAI (Grok) API <a class="header-anchor" href="#xai-grok-api" aria-label="Permalink to “xAI (Grok) API”">​</a></h3><p>Same shape: the remote MCP tool takes <code>authorization</code> (raw token) or a <code>headers</code> map. No OAuth exists on this surface.</p><h3 id="gemini-api-mcp-server-tool" tabindex="-1">Gemini API (<code>mcp_server</code> tool) <a class="header-anchor" href="#gemini-api-mcp-server-tool" aria-label="Permalink to “Gemini API (mcp_server tool)”">​</a></h3><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">{</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">  &quot;type&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;mcp_server&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">,</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">  &quot;name&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;mcp_hub&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">,</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">  &quot;url&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;https://mcp.example.net/hub&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">,</span></span>
<span class="line"><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">  &quot;headers&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: { </span><span style="--shiki-light:#116329;--shiki-dark:#7EE787;">&quot;Authorization&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">&quot;Bearer &lt;token&gt;&quot;</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;"> }</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">}</span></span></code></pre></div><p>Note Gemini&#39;s own constraint: the tool <code>name</code> must not contain <code>-</code> — use <code>mcp_hub</code>, not <code>mcp-hub</code>.</p><h3 id="header-only-chat-clients" tabindex="-1">Header-only chat clients <a class="header-anchor" href="#header-only-chat-clients" aria-label="Permalink to “Header-only chat clients”">​</a></h3><p>Qwen Chat (via an <code>mcp-remote --header</code> stdio shim), older Cline and Windsurf builds, and Gemini CLI/Codex in header mode all take the same <code>Authorization: Bearer &lt;token&gt;</code> header.</p><h2 id="camp-3-—-pre-registered-oauth-clients-mint-the-client-yourself" tabindex="-1">Camp 3 — pre-registered OAuth clients (mint the client yourself) <a class="header-anchor" href="#camp-3-—-pre-registered-oauth-clients-mint-the-client-yourself" aria-label="Permalink to “Camp 3 — pre-registered OAuth clients (mint the client yourself)”">​</a></h2><p>Grok&#39;s web connectors, Gemini Enterprise, Copilot Studio&#39;s manual mode and Microsoft Agent 365 do no dynamic registration: they expect a client_id and client_secret you enter on both sides. Since 0.10.0 the hub can mint one:</p><div class="language-bash"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#953800;--shiki-dark:#FFA657;">mcp-hub-admin</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> clients</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> add</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF;"> --name</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> &quot;Gemini Enterprise&quot;</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;"> \\</span></span>
<span class="line"><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF;">  --redirect-uri</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;"> https://example.com/oauth/callback</span></span></code></pre></div><p>The command prints the <code>client_id</code> and — this once — the <code>client_secret</code>, then paste both into the connector along with the hub&#39;s authorization and token endpoints. Add <code>--public</code> for a connector that has no secret to store; it then authenticates with PKCE alone. Operator-minted clients count as approved the moment you create them (you named the redirect URI, so there is nothing left to confirm in a browser) and are exempt from the <a href="/guide/client-registration#registrations-do-not-accumulate">pruning rules</a> — one you typed out by hand must not vanish after ninety idle days. See the <a href="/reference/admin-cli">admin CLI reference</a>.</p><p>Two caveats for this camp:</p><ul><li>The token endpoint reads the secret from the request body (<code>client_secret_post</code>), which is what the AS metadata advertises. A connector that can only send HTTP Basic credentials will not authenticate.</li><li>PKCE is mandatory, with no per-client opt-out. Gemini Enterprise leaves it as a checkbox — tick it.</li></ul><p>Copilot Studio generates its redirect URI only <em>after</em> you create the tool, so create the tool first and mint the client second.</p><h2 id="products-with-no-custom-mcp-client-at-all" tabindex="-1">Products with no custom-MCP client at all <a class="header-anchor" href="#products-with-no-custom-mcp-client-at-all" aria-label="Permalink to “Products with no custom-MCP client at all”">​</a></h2><p>The Kimi consumer app, the Moonshot K2 API and DeepSeek currently have no way to add a custom remote MCP server. Nothing the hub can do — reach those models through a third-party client from camp 1 instead.</p>`,37)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};